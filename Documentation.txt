clear - очищает консоль
print - выводит(что-то)
color red - меняет цвет на красный
color green - меняет цвет на зелёный
color yellow - меняет цвет на красный
color blue - меняет цвет на синий
color pink - меняет цвет на розовый
color black - меняет цвет на чёрный
color white - меняет цвет на белый
color reset - меняет цвет на начальный
color cyan - меняет цвет на голубой
time(now) - показывает настоящее время
time - ввод даты
input - ввод
hw - выводит Hello world!
let - создаёт переменную
delete let(let) - удаляет переменную
println() - выводит на новой строке(как в JS)
message.send() - отправляет сообщение на язык
art.print() - выводит большими буквами
global(let) - создаёт глобальную переменную
df() - удаляет папку(путь к папке(для удаления))
pay - плятёж на язык(не деньгами(electro-коинами))
pass - ожидание написания команды
df(f) - удаляет папку и файлы в ней(путь к папке(для удаления))
df(file) - удаляет файл(путь к файлу(для удаления))
site - открывает сайт проекта
print(let) - выводит переменную
println(let) - выводит переменную на новую строку
import - импортирует библиотеку
print(imports) - выводит все скачанные библиотеки
println(imports) - выводит все скачанные библиотеки на новую строку
gp() - ввод без возможности смотреть что вы вводите, пароль(библиотека gp)
web - открывает страницу
web_new - открывает новую страницу
web_new_tab - открывает новую страницу в новом окне
quit - выходит из редактора
nf - создаёт новую папку
nf(f) - создаёт новый файл
cd - переходит в папку
let size - узнаёт размер переменной
math.pi - выводит число ПИ
math.calc - создаёт калькулятор в языке
infinite_fone - выводит бесконечное количество слов
server.create(local) - создаёт локальный сервер
print(server(local)) - выводит локальный сервер
println(server(local)) - выводит локальный сервер на новую строку
license - открывает лицензию
list - создаёт список
console.error() - создаёт ошибку в консоли
console.info() - создаёт информации о консоли
console.warn() - выдаёт предупреждение в консоли
print(list) - выводит список
server.crete(global) - создаёт глобальный сервер
prinln(list) - выводит список на новой строке
pip(linux) - выводит информацию о установке на Linux
pip(windows) - выводит информацию о установке на Windows
pip(macos) - выводит информацию о установке на MacOS
AVATAR - показывает аватар языка
text - редактор текста
ai.create(new) - создаёт чат GPT
ai.del() - удаляет возможность пользоваться чат GPT
ai.stop() - тоже самое что и ai.del()
file.open(new) - создаёт новый файл(и даёт возможность написать в него текст)
off/on - перезагружает компьютер
off(+) - даёт возможность выключить компьютер(указанное количество минут)
off() - выключает компьютер
let+ - добавляет к переменной указанные символы
let- - удаляет из переменной указанные символы
img.open() - открывает изображение(путь к файлу(для показа))
bg green - зелёный фон
bg red - красный фон
bg blue - синий фон
bg pink - розовый фон
bg cyan - голубой фон
bg yellow - жёлтый фон
bg black - чёрный фон
bg white - белый фон
bg reset - начальный фон